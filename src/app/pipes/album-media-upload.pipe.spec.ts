import { AlbumMediaUploadPipe } from './album-media-upload.pipe';

describe('AlbumMediaUploadPipe', () => {
  it('create an instance', () => {
    const pipe = new AlbumMediaUploadPipe();
    expect(pipe).toBeTruthy();
  });
});
